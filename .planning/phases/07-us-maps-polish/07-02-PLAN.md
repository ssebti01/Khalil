# Plan 07-02: Integration Smoke Test & Polish Pass

## Goal

Verify that all 7 maps (default stadium, Rabat, Bouskoura, Shanghai, NYC, Chicago, Houston) load and play correctly end-to-end. Confirm Phase 1–3 fixes (goal physics, movement drift, pause menu) remain intact across every map. Fix any regressions or polish issues found during the smoke test and document results.

## Context

By this plan, Phases 1–6 and Plan 07-01 are complete. The following subsystems must function on every map:

- **Phase 1 fixes:** Ball rolls cleanly into goal without stopping; player velocity zeroes correctly on key release.
- **Phase 2:** Head sprites display at increased size with spring wobble on ball contact, jump landing, and ability activation.
- **Phase 3:** ESC opens pause overlay; Resume restores state; Restart resets same config; Return to Menu navigates correctly. Timer and cooldowns freeze during pause.
- **Phase 4:** Map selector in MenuScene shows all 7 maps; selecting any map passes `mapId` to GameScene; MapLoader creates correct physics bodies.
- **Phase 5–6–07-01 maps:** Backgrounds render, obstacles are solid, special zones (NYC trampoline, Chicago wind) fire correctly.

**7 map IDs to test (in order):** `default`, `rabat`, `bouskoura`, `shanghai`, `nyc`, `chicago`, `houston`.

**Test matrix per map:**
| Check | How |
|---|---|
| Map loads without console errors | Browser DevTools console |
| Background renders (not blank/black) | Visual |
| Obstacles solid (ball deflects) | Kick/roll ball into each obstacle |
| Special zones fire (NYC trampoline, Chicago wind) | Ball enters zone |
| Goal scores correctly | Kick ball in from centre |
| Pause works (ESC, all 3 buttons) | Press ESC mid-match |
| CPU plays without errors (1P mode) | 30-second CPU match |
| 2P mode playable | Manual two-key test |
| Match ends cleanly (ResultScene) | Let score reach 7 or time run out |

**Punch list:** Findings are written to `.planning/phases/07-us-maps-polish/07-02-PUNCH-LIST.md` and fixed in-place before this plan is marked complete.

## Must Haves

- [ ] All 7 maps appear in map selector and each loads without JavaScript console errors
- [ ] Scoring works correctly on all 7 maps (ball enters goal → score increments → GOAL! flash)
- [ ] Phase 1 goal physics fix holds on all maps (no stop-dead-at-goal regression)
- [ ] Phase 1 movement drift fix holds (player stops when key released) on all maps
- [ ] Phase 2 head wobble fires on all maps (head visibly squishes on ball contact)
- [ ] Phase 3 pause ESC works on all maps (game freezes, all 3 buttons function)
- [ ] Chicago wind applies on Chicago only (no unexpected drift on other maps)
- [ ] NYC trampoline fires correctly (ball bounces upward when crossing grate zone)
- [ ] Houston floor noticeably bouncier than default (visual confirmation)
- [ ] ResultScene shows correct winner after match ends on every map

## Tasks

### Task 1: Execute smoke test matrix and write punch list

**File:** `.planning/phases/07-us-maps-polish/07-02-PUNCH-LIST.md` (create)

**Action:** Run `npm run dev`. For each of the 7 maps, work through the test matrix above. For each failure or visual defect found, record it in the punch list file with: map ID, symptom, likely cause, proposed fix. If no issues are found for a map, record "PASS" for that map row.

The punch list format:

```markdown
# 07-02 Punch List

## Test Date: [date]

| Map | Goal | Pause | P1 Drift | Head Wobble | Obstacles | Special Zone | CPU OK | 2P OK | Result Screen | Notes |
|-----|------|-------|----------|-------------|-----------|--------------|--------|-------|---------------|-------|
| default   | | | | | | N/A | | | | |
| rabat     | | | | | | N/A | | | | |
| bouskoura | | | | | | N/A | | | | |
| shanghai  | | | | | | N/A | | | | |
| nyc       | | | | | trampoline | | | | |
| chicago   | | | | | wind gust | | | | |
| houston   | | | | | ramp+barrels | N/A | | | |

## Issues Found

### [MAP-ID]-[N]: [Short title]
- **Symptom**: [What went wrong]
- **Reproduction**: [Steps]
- **Likely cause**: [Code location]
- **Fix**: [Proposed change]

## Resolved Issues

(Move items here after fixing)
```

Fill PASS / FAIL / SKIP in each cell. List issues in the Issues Found section.

**Verify:** File exists at `.planning/phases/07-us-maps-polish/07-02-PUNCH-LIST.md` and contains the completed table (no empty cells).

### Task 2: Fix all punch-list issues

**File:** Whichever source files the punch list identifies (may include `src/config/maps.js`, `src/scenes/GameScene.js`, `src/systems/MapLoader.js`, `src/scenes/MenuScene.js`, `src/entities/Player.js`, `src/entities/Ball.js`)

**Action:** Work through every FAIL item in the punch list one by one. For each issue:
1. Identify the root cause in source code.
2. Apply the minimal targeted fix.
3. Re-run the specific failing test check to confirm resolution.
4. Move the issue from "Issues Found" to "Resolved Issues" in the punch list with a note of what was changed.

If zero issues were found in Task 1, skip this task (punch list will show all PASS).

**Common regression patterns to watch for and their fixes:**

| Symptom | Likely location | Fix |
|---|---|---|
| Ball stops at goal mouth | `GameScene._createWalls()` — back wall too close | Verify Phase 1 fix values still present (`-thick` not `-thick/2`) |
| Player drifts after key release | `Player.js` velocity zero logic | Verify Phase 1 drift fix — `setVelocityX(0)` on no-key frame |
| Head wobble absent | `Player.js` wobble trigger | Check `emitWobble` events still fire from Ball collision handler |
| Pause does not freeze physics | `PauseScene.js` or `GameScene.js` | Verify `this.matter.world.pause()` / `resume()` pair |
| Map obstacles missing | `MapLoader.js` obstacle factory | Check `type` switch cases cover all types used in new maps |
| Wind fires on wrong map | `GameScene._setupWind()` | Ensure guard `if (!mapConfig?.windForce) return` is present |
| Trampoline silent | `MapLoader.js` special zone handler | Verify trampoline sensor collision callback applies `impulseY` |
| Map selector missing new maps | `MenuScene.js` | Confirm selector iterates full `MAPS` array length |
| ResultScene wrong winner | `GameScene._endMatch()` | Check score comparison logic unchanged |

**Verify:** Re-run smoke test for every map that had a FAIL. All cells in punch list table now show PASS. Zero JavaScript console errors during a complete match on any map.

## Verification

1. Run `npm run dev`, confirm dev server starts with zero build errors (`npm run build` must also exit 0).
2. Open map selector — count exactly 7 map entries visible (default, Rabat, Bouskoura, Shanghai, NYC, Chicago, Houston).
3. Play one complete match (to score limit or time limit) on every map in 1P vs CPU mode — confirm GOAL!, score counter, and ResultScene all work.
4. On each map, press ESC mid-match — pause overlay appears. Press Resume — game continues. Confirm timer was frozen (time did not count down during pause).
5. On NYC: push ball across the grate zone (x 560–720, y≈630) — ball bounces upward.
6. On Chicago: wait 4+ seconds during play — observe ball drifting horizontally for ~0.5s (wind gust). Drift stops. Cycle repeats.
7. On Houston: let ball drop from height onto floor — visibly higher bounce than on default map floor.
8. On default map: score a goal by rolling ball cleanly through goal mouth from open field — no stop-dead. Score increments.
9. Open browser DevTools console after all 7 map tests — zero uncaught errors, zero warnings about undefined properties on map config.
10. Review `.planning/phases/07-us-maps-polish/07-02-PUNCH-LIST.md` — all rows show PASS, Issues Found section is empty or all items moved to Resolved.
